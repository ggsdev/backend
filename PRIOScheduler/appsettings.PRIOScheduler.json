{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "dbConnection": "server=${SERVER},${PORT}\\${SERVER_INSTANCE}; database=${DATABASE}; User ID=${USER_ID}; Password=${PASSWORD}; Encrypt=${ENCRYPT}"
  },
  "AllowedHosts": "*"
}
